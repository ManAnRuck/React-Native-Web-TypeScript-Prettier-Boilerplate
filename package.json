{
    "private": true,
    "workspaces": {
        "packages": [
            "packages/*"
        ],
        "nohoist": [
            "**/react-native",
            "**/react-native/**",
            "**/common",
            "**/react-native-typescript-transformer",
            "**/react-native-typescript-transformer/**"
        ]
    },
    "scripts": {
        "dev": "lerna run --parallel dev",
        "lint": "lerna run --parallel lint",
        "build": "yarn --cwd packages/web build",
        "start": "yarn --cwd packages/web start",
        "pre-commit": "lerna run --parallel pre-commit"
    },
    "name": "app",
    "devDependencies": {
        "lerna": "^3.2.1",
        "tslint": "^5.11.0"
    }
}
